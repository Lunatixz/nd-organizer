# Vendored Navidrome Rust PDK

Navidrome's Rust plugin development kit crates, vendored from
`navidrome/navidrome` (`plugins/pdk/rust`). Referenced via path dependencies in
the plugin's `Cargo.toml`.

Crates:
- `nd-pdk` — umbrella crate
- `nd-pdk-host` — host function wrappers (Navidrome services)
- `nd-pdk-capabilities` — capability traits + `register_*` macros
- `nd-pdk-types` — shared data types

Update by copying from the upstream repo. Do not edit generated files
("Code generated by ndpgen").

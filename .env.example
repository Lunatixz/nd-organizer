# Copy to `.env` next to docker-compose.yml and fill in what you use.
# Compose interpolates ${VAR:-default} from this file.

# ===== MySQL (optional - for persistent plugin state) =====
# Only needed if you uncomment nd-organizer-mariadb in docker-compose.yml
MYSQL_ROOT_PASSWORD=ndorganizer
MYSQL_PASSWORD=ndorganizer

# ===== Navidrome =====
# ND_MUSICFOLDER=/music
# ND_DATAFOLDER=/data
